# Python dependencies for Talk With Zeno backend
# Install with: pip install -r backend/requirements.txt

# Web framework
flask>=3.0.0
flask-cors>=4.0.0

# Environment variable management
python-dotenv>=1.0.0

# HTTP requests (for Google OAuth)
requests>=2.31.0

# Google Cloud services
google-cloud-speech>=2.21.0
google-generativeai>=0.3.0  # DEPRECATED: Will migrate to google-genai in future update
# google-genai>=1.0.0  # New package (migration pending)

# Groq SDK for TTS and STT (Whisper models)
groq>=1.0.0

# Error monitoring and logging (optional - backend works without it)
# sentry-sdk[flask]>=2.0.0  # Optional: Install only if you want Sentry error monitoring

# Voice Activity Detection (optional, for backend VAD)
# silero-vad>=4.0.0  # Highest accuracy, ML-based (recommended)
# torch>=2.0.0  # Required for silero-vad
webrtcvad>=2.0.10  # Faster fallback option (lighter, less accurate) - INSTALLED
setuptools>=80.0.0  # Required for webrtcvad

# Audio processing for WebM to PCM conversion
pydub>=0.25.1  # For converting WebM/Opus to PCM for VAD

# Rate limiting
flask-limiter>=3.5.0

# System monitoring
psutil>=5.9.0

# Note: SQLite is built into Python, no installation needed!
# Note: Groq API key needed for TTS and STT (set GROQ_API_KEY in .env.local)
# Note: Google credentials needed for STT fallback (set GOOGLE_APPLICATION_CREDENTIALS)
# Note: webrtcvad is installed and working (requires Microsoft C++ Build Tools on Windows)

